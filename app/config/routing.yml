app_membership_extra:
    resource: "@AppBundle/Controller/MembershipExtraController.php"
    type:     annotation

admin:
    resource: "@AdminBundle/Controller/"
    type:     annotation
    prefix:   /admin

app:
    resource: "@AppBundle/Controller/"
    type:     annotation

logout:
    path: /logout
