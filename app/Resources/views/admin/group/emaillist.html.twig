{% extends 'adminbase.html.twig' %}

{% block breadcrumbs %}
    <nav class="breadcrumb">
        <a class="breadcrumb-item" href="/">Home</a>
        <a class="breadcrumb-item" href="{{ path('admin_index')}}">Admin</a>
        <a class="breadcrumb-item" href="{{ path('admin_group_index')}}">Group</a>
        <a class="breadcrumb-item" href="{{ path('admin_group_show', { id: group.id } )}}">{{ group.name }}</a>
        <span class="breadcrumb-item active">Email List</span>
    </nav>
{% endblock %}

{% block body %}
    <h1>{{ group.name }}</h1>

    <h3>Regular Emails</h3>
    {{ regularEmailsString }}

    <h3>Crappy BT Emails</h3>
    {{ btEmailsString }}

{% endblock %}
