{% extends 'base.html.twig' %}

{% block body %}
    <h1>Qualificationcategory</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qualificationCategory.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ qualificationCategory.category }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_qualificationcategory_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_qualificationcategory_edit', { 'id': qualificationCategory.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
