{% extends 'base.html.twig' %}

{% block body %}
    <h1>ParticipantRole</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participantRole.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ participantRole.role }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_participantrole_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_participantrole_edit', { 'id': participantRole.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
