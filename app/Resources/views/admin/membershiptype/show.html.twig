{% extends 'base.html.twig' %}

{% block body %}
    <h1>MembershipType</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membershipType.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ membershipType.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_membershiptype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_membershiptype_edit', { 'id': membershipType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
