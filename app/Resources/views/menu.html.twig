<nav class="navbar navbar-toggleable-md navbar-inverse bg-inverse">

    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#topNavBar" aria-controls="topNavBar" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <a class="navbar-brand" href="{{ path('homepage') }}">{{ site_name }}</a>

    <div class="collapse navbar-collapse" id="topNavBar">

    <ul class="nav navbar-nav mr-auto">
        <li class="nav-item">
            <a class="nav-link" href="{{ path('centre') }}">Eagle Canoe Centre</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">How To Join</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('activity_index')}}">Activities</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">Pics &amp; Videos</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('faqs') }}">FAQs</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('contact_index')}}">Contact</a>
        </li>
    </ul>

    <ul class="nav navbar-toggleable-md navbar-nav mr-0">
        {% if app.user %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_index') }}"><i class="fa fa-cogs"></i> Admin</a>
                </li>
            {% endif %}

            <li class="nav-item dropdown">
                <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                    <i class="fa fa-user"></i> {{ app.user.forename }} <span class="caret"></span></a>

                <div class="dropdown-menu" aria-labelledby="dropdownMenu2">
                    <a href="{{ path('account_overview') }}" class="dropdown-item"><i class="fa fa-user"></i> Overview</a>
                    <a href="{{ path('account_activities') }}" class="dropdown-item"><i class="fa fa-map"></i> Activities</a>
                    <a href="{{ path('account_edit') }}" class="dropdown-item"><i class="fa fa-pencil"></i> Profile</a>
                    <div class="dropdown-divider"></div>
                    <a href="{{ path('logout') }}" class="dropdown-item"><i class="fa fa-sign-out"></i> Logout</a>
                </div>
            </li>
        {% else %}
            <li class="nav-item">
                <a href="{{ path('register') }}" class="nav-link"><i class="fa fa-user-plus"></i> Register</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('login') }}" class="nav-link"><i class="fa fa-sign-in"></i> Login</a>
            </li>
        {% endif %}
    </ul>

</div>
</nav>
